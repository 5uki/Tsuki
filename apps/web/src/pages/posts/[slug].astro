---
import Default from '@/layouts/Default.astro'

export async function getStaticPaths() {
  return []
}

const { slug } = Astro.params
---

<Default title={`文章: ${slug}`} description="文章详情">
  <article class="post">
    <header class="mb-[var(--tsuki-space-2)] border-b border-[var(--tsuki-surface-border)] pb-[var(--tsuki-space-1)]">
      <h1 class="mb-[var(--tsuki-space-0-5)] text-[length:var(--tsuki-text-3xl)]">{slug}</h1>
      <p class="text-[length:var(--tsuki-text-sm)] text-[color:var(--tsuki-fg)]">文章详情页占位</p>
    </header>
    <div class="prose">
      <p>这是文章 "{slug}" 的详情页。M1 将实现完整功能。</p>
    </div>
  </article>
</Default>
