---
import Default from '@/layouts/Default.astro'

export async function getStaticPaths() {
  return []
}

const { slug } = Astro.params
---

<Default title={`文章: ${slug}`} description="文章详情">
  <article class="post">
    <header class="mb-8 border-b border-[color-mix(in_srgb,var(--tsuki-theme-secondary)_64%,var(--tsuki-color-on-primary))] pb-4">
      <h1 class="mb-2 text-[var(--tsuki-text-6xl)]">{slug}</h1>
      <p class="text-[var(--tsuki-text-base)] text-[var(--tsuki-fg)]">文章详情页占位</p>
    </header>
    <div class="prose">
      <p>这是文章 "{slug}" 的详情页。M1 将实现完整功能。</p>
    </div>
  </article>
</Default>
