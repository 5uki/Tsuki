---
import Default from '@/layouts/Default.astro'

export async function getStaticPaths() {
  return []
}

const { id } = Astro.params
---

<Default title="动态" description="动态详情">
  <article class="moment">
    <header class="mb-[var(--tsuki-space-1)]">
      <p class="text-[length:var(--tsuki-text-sm)] text-[color:var(--tsuki-fg)]">动态详情页占位</p>
    </header>
    <div class="prose">
      <p>这是动态 "{id}" 的详情页。M2 将实现完整功能。</p>
    </div>
  </article>
</Default>
