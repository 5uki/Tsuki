---
import Default from '@/layouts/Default.astro'

export async function getStaticPaths() {
  return []
}

const { slug } = Astro.params
---

<Default title={`鍒嗙粍: ${slug}`} description={`鍒嗙粍 ${slug} 涓嬬殑鏂囩珷`}>
  <h1 class="mb-[var(--tsuki-space-6)] text-[var(--tsuki-text-5xl)]">{slug}</h1>
  <p class="italic text-[var(--tsuki-fg)]">璇ュ垎缁勪笅鏆傛棤鏂囩珷</p>
</Default>
